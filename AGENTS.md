# AI Engineering Harness 开发约定

本仓库构建跨模型、跨角色、跨技术栈的 AI 工程协作 Harness。

## 开始工作

1. 阅读 `docs/plan.md`，确认当前阶段和非目标。
2. 检查相关源码、测试与 `git status`，保留已有修改。
3. 修改协议时同步检查 Schema、CLI、Skill 和测试的兼容性。

## 核心边界

- Kernel 只定义任务、结果、权限、证据和生命周期，不包含具体业务或框架知识。
- Skill 按能力和工作流拆分，不创建覆盖所有场景的万能 Skill。
- 角色是 Skill 与权限的组合，不复制 Skill 内容。
- 具体项目拥有产品事实、架构、命令和安全策略；Harness 不覆盖这些事实。
- 供应商适配层保持薄，不在 Codex、Claude 或 Copilot 适配器中实现业务流程。
- 可确定执行的行为优先写成 CLI、Schema 或测试，不依赖提示词约束。
- 默认不允许 Skill 隐式扩大文件写入、网络、提交、部署或外部系统权限。

## 实现约定

- MVP 使用 Node.js 18+ 和标准库，未经评审不引入运行时依赖。
- 公共数据格式必须版本化，并为破坏性变化提供迁移策略。
- CLI 错误信息应说明原因和可执行的修复路径。
- 行为测试验证结果和边界，不匹配模型生成的固定措辞。
- 不为尚未验证的角色、技术栈或编排平台创建空目录与占位实现。

## 完成前

执行：

```bash
npm run check
git diff --check
```

说明改动、验证结果、兼容性影响和剩余限制。未经明确要求，不 Commit、Push、发布包或修改外部系统。
